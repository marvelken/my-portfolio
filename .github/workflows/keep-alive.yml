name: Keep Upstash Redis Alive

on:
  schedule:
    # Runs every Sunday at midnight UTC
    - cron: '0 0 * * 0'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check endpoint
        run: curl -s "${{ secrets.HEALTH_CHECK_URL }}" || exit 0
